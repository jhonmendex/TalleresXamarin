<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ejercicioOnPlatform.StaticRef"
             xmlns:local="clr-namespace:ejercicioOnPlatform.clases">
    <ContentPage.Content>
        <StackLayout Padding="10">
           <Label TextColor="{x:Static local:AppConstants.ForegroundColor}"
                  Text="Ejemplo static"
                  BackgroundColor="{x:Static local:AppConstants.BackgroundColor}"
                  Font="{x:Static local:AppConstants.TitleFont}"
                  Padding="{x:Static local:AppConstants.PagePadding}" 
                  />
                    <BoxView WidthRequest="{x:Static sys:Math.PI}" 
               HeightRequest="{x:Static sys:Math.E}" 
               Color="{x:Static local:AppConstants.ForegroundColor}" 
               HorizontalOptions="Center" 
               VerticalOptions="CenterAndExpand" 
               Scale="100" /> 

        </StackLayout>
       
    </ContentPage.Content>
</ContentPage>
