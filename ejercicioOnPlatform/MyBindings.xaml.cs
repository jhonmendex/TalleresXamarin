using System;
using System.Collections.Generic;
using ejercicioOnPlatform.clases;
using Xamarin.Forms;

namespace ejercicioOnPlatform
{
    public partial class MyBindings : ContentPage
    {
        public MyBindings()
        {
            InitializeComponent();
            //lblMessage.BindingContext = new DataBindingSource();
            //lblMessage.SetBinding(Label.TextProperty, "Message");
        }
    }
}
